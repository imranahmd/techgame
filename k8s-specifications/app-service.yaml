apiVersion: v1
kind: Service
metadata:
  name: techgame
spec:
  selector:
    app: techgame
  ports:
    - protocol: TCP
      port: 5000       # External service port
      targetPort: 5000  # Must match Deployment
  type: LoadBalancer











#apiVersion: v1
#kind: Service
#metadata:
  #labels:
    #app: techgame
  #name: techgame
#spec:
  #type: NodePort
  #ports:
  #- name: "techgame-service"
   # port: 5000
    ##targetPort: 80
    #nodePort: 31005
  #selector:
    #app: techgame
